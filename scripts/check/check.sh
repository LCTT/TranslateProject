#!/bin/sh
# 检查脚本状态
set -e

################################################################################
# 暂时仅供开发使用
################################################################################

shellcheck -e SC2034 -x mock/stats.sh "$(dirname "$0")"/*.sh \
  && echo '[检查] ShellCheck 通过'
